/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
    domains: ['images.unsplash.com','via.placeholder.com','assets.aceternity.com'], // Add the domain here
  },
  
};

export default nextConfig;
